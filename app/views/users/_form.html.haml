
- if @user.errors.any?
  #error_explanation
    %h2= "#{pluralize(@user.errors.count, "error")} prohibited this user from being saved:"
    %ul
      - @user.errors.full_messages.each do |msg|
        %li= msg

.field
  = f.input :email
.field
  = f.input :password
.field
  = f.input :password_confirmation
.field
  = f.input :amount
.field
  = f.input :username

= f.input :seller, label: false, :input_html => { :checked => seller, class: "hidden" }


